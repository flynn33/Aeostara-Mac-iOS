# Aeostara iOS Build & Test
# Proves the native Swift/SwiftUI implementation builds and tests on Apple hardware.
# Copyright (c) 2026 James Daley. All Rights Reserved.

name: iOS Build & Test

on:
  push:
    branches: [platform/ios]
  pull_request:
    branches: [platform/ios]

jobs:
  build-and-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build
        run: swift build

      - name: Run tests
        run: swift test
